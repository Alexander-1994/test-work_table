import loading from '../sources/loading.gif';

const Loading = () => {
    return (
        <div className='universal-table__loading'>
            <img src={loading} alt="loading" />
		</div>
    )
};

export default Loading;